name: Setup
description: Setup Env

runs:
  using: composite
  steps:
    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: lx-music-mobile-v${{ env.PACKAGE_VERSION }}
        path: android/app/build/outputs/apk/{release,debug}/*.apk
